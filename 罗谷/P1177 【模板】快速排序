#include<bits/stdc++.h>
using namespace std;
priority_queue<int,vector<int>,greater<int> > q;
int main(){
    int num;
    cin>>num;
    for (int i=1;i<=num;i++){
        int x;
        cin>>x;
        q.push(x);
    }
    while (!q.empty()){
        cout<<q.top()<<' ';
        q.pop();
    }
}
